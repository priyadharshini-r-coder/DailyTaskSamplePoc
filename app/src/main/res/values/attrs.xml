<?xml version="1.0" encoding="utf-8"?>
<resources>
    <declare-styleable name="PinEntryView">
        <!-- The number of digits in the pin -->
        <attr name="numDigits" format="integer" />
        <!-- Digit dimensions, default 50dp x 50dp -->
        <attr name="digitWidth" format="dimension" />
        <attr name="digitHeight" format="dimension" />
        <!-- Spacing between the pin boxes, default 20dp -->
        <attr name="digitSpacing" format="dimension" />
        <!-- Digit background, colour or drawable -->
        <attr name="digitBackground" format="reference|color" />
        <!-- Font size to use for the mask text, default 15sp -->
        <attr name="digitTextSize" format="dimension" />
        <!-- Colour of the mask text, defaults to theme textColorPrimary -->
        <attr name="digitTextColor" format="color" />
        <!-- Digit elevation (API 21+ only), default 0dp -->
        <attr name="digitElevation" format="dimension" />
        <!-- The character to use to mask the input -->
        <attr name="mask" format="string" />
        <!-- Accent color, defaults to theme colorAccent -->
        <attr name="pinAccentColor" format="color" />
        <!-- Accent width, default 3dp -->
        <attr name="accentWidth" format="dimension" />

        <attr name="strokewidth" format="dimension" />
        <attr name="strokeRadius" format="dimension" />
        <!-- Accent type, default 'character' -->
        <attr name="accentType" format="enum">
            <enum name="none" value="0" />
            <enum name="all" value="1" />
            <enum name="character" value="2" />
        </attr>
        <!-- Input type -->
        <attr name="pinInputType" format="enum">
            <enum name="number" value="2" />
        </attr>
        <!-- Only draw accent when View has focus -->
        <attr name="accentRequiresFocus" format="boolean" />
    </declare-styleable>

    <declare-styleable name="CircularImageView"><!-- Specifies whether or not to draw a
                                                                    circular border around the image. -->

        <attr name="border" format="boolean" /><!-- Specifies the color of the border draw
                                                                        around the image. (if enabled) -->

        <attr name="border_color" format="color" /><!-- Makes the border this pixels wide. (if enabled) -->

        <attr name="border_width" format="dimension" /><!-- Specifies whether or not to draw a
                                                            selector on this view upon touch events. -->

        <attr name="selector" format="boolean" /><!-- Specifies the color of the selector draw
                                                    on top of the image upon touch events. (if enabled) -->

        <attr name="selector_color" format="color" /><!-- Specifies the color of the selector
           stroke drawn around the image upon touch events. Be sure to provide some opacity. (if enabled) -->

        <attr name="selector_stroke_color" format="color" /><!-- Makes the selector stroke drawn
                            around the image upon touch events this pixels wide. (if enabled) -->

        <attr name="selector_stroke_width" format="dimension" /><!-- Specifies whether or not to
                                                            draw a shadow around your circular image. -->
        <attr name="shadow" format="boolean" />
        <attr name="border_overlay" format="boolean" />
        <attr name="fill_color" format="color" />
    </declare-styleable>
</resources>